import { Component } from '@angular/core';

@Component({
  selector: 'app-resolution-dprovider',
  template: `
    <app-resolution-dprovider-header></app-resolution-dprovider-header>
    <router-outlet></router-outlet>
  `,
})
export class ResolutionDProviderComponent {}
